{
    "name": "personal-brand-website",
    "version": "0.1.0",
    "description": "Personal brand website — Astro + Starlight",
    "type": "module",
    "license": "MIT",
    "engines": {
        "node": ">=22"
    },
    "scripts": {
        "sync-spec": "node scripts/sync-spec.mjs",
        "fetch-refs": "node scripts/fetch-refs.mjs",
        "generate-markdown-map": "node scripts/generate-markdown-map.mjs",
        "dev": "npm run sync-spec && npm run generate-markdown-map && astro dev",
        "build": "npm run sync-spec && npm run fetch-refs && npm run generate-markdown-map && astro build",
        "preview": "astro preview"
    },
    "dependencies": {
        "@astrojs/rss": "^4.0.18",
        "@astrojs/starlight": "^0.38.2",
        "astro": "^6.4.6",
        "rehype-mermaid": "^3.0.0",
        "remark-gfm": "^4.0.1",
        "sharp": "^0.34.2"
    },
    "overrides": {
        "esbuild": "^0.28.1"
    },
    "devDependencies": {
        "playwright": "1.60.0"
    }
}

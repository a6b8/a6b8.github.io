{
    "name": "personal-brand-website",
    "version": "0.1.0",
    "description": "Personal brand website — Astro + Starlight",
    "type": "module",
    "license": "MIT",
    "engines": {
        "node": ">=22"
    },
    "scripts": {
        "sync-spec": "node scripts/sync-spec.mjs",
        "fetch-refs": "node scripts/fetch-refs.mjs",
        "dev": "npm run sync-spec && astro dev",
        "build": "npm run sync-spec && npm run fetch-refs && astro build",
        "preview": "astro preview"
    },
    "dependencies": {
        "@astrojs/rss": "^4.0.18",
        "@astrojs/starlight": "^0.38.2",
        "astro": "^6.4.6",
        "sharp": "^0.34.2"
    },
    "overrides": {
        "esbuild": "^0.28.1"
    }
}

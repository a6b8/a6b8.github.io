# Personal Brand website

A minimal personal brand website built with [Astro](https://astro.build) and [Starlight](https://starlight.astro.build).

Structure: a splash landing page, a Docs section, and a Blog (with RSS).

```bash
npm install   # install dependencies
npm run dev   # local dev server
npm run build # production build -> dist/
```
